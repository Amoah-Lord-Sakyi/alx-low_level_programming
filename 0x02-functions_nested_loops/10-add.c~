#include "main.h"
/**
 * add - adds two integers and returns the result.
 *@a: An input integer
 *@b: An input integer
 * Return: the result.
*/
int add(int a, int b)
{
int result;
result = a +b;
return (result);
}
