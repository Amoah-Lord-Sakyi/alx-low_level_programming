#include "main.h"
/**
 * print_alphabet - prints all  alphabet, in lowercase.
 *
 * Return: Always 0 (success)
*/

void print_alphabet(void)
{
char s;
for (s = 'a' ; s <= 'z' ; s++;)
_putchar(s);
_putchar('\n');
}
