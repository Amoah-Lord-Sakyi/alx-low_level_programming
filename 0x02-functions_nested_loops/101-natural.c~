#include <stdio.h>
/**
 *main - entry point
 *Return: Always 0 (success)
*/
int main(void)
{
int x, sum = 0;
for (x = 0; x < 1024; x++)
if ((x % 3) == 0 || (x % 5) == 0)
sum += 1;
{
putchar("sum\n");
}
return (0);
}
