#include <stdio.h>
/**
* main-the code entry
* return (0): returns to main after the code has been executed
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzl\n");
return (0);
}
