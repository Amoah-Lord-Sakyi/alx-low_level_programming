#include <stdio.h>
/**
 *
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char i;
char a;
for (i =  'a';  i <=  'z';  i++);
{
putchar(char *i);
}
for (a =  'A';  a <=  'Z'; a++ )
{
putchar(char *j);
}
return (0);
}
