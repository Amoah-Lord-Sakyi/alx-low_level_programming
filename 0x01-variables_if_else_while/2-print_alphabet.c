#include <stdio.h>
/**
 * main - Entry piont
 *
 * Return: 0 if no error, non zero if error
 */
int main(void)
{
char i;
char c;
i = 'a';
while
(i <=  'z')
{
putchar(i);
c++;
}
putchar('\n');
return (0);
}
